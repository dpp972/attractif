{% extends 'MainBundle:Front:baseFront.html.twig' %}

{% block title %}ATTRACTIF{% endblock %}

{% block content %}

<section>
    <form class="form-account" method="post" action="{{ url('account') }}">
        <a class="insc membre" href="#">Dejà membre?</a>
        <div class="contenu">
            <div class="form-group">
                <label>pays :</label>
                <input type="text" value="{{ user.nom }}">
            </div>
            <div class="form-group">
                <label>Nom :</label>
                <input type="text" value="{{ user.nom }}">
            </div>
            <div class="form-group">
                <label>Prenom :</label>
                <input type="text" value="{{ user.prenom }}">
            </div>
            <div class="form-group">
                <label>Née le : </label>
                <input type="text" value="">
            </div>
            <div class="form-group">
                <label>Code Postal :</label>
                <input type="text" value="{{ user.cp }}">
            </div>
            <div class="form-group">
                <label>Ville :</label>
                <input type="text" value="{{ user.ville }}">
            </div>
            <div class="form-group">
                <label>Email :</label>
                <input type="text" value="{{ user.email }}">
            </div>
        </div>
        <input class="submit" type="submit" value="Inscription">
    </form>

</section>    

{% endblock %}
