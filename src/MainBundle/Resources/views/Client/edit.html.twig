{% extends 'MainBundle:Back:baseBack.html.twig' %}
{% form_theme edit_form 'MainBundle:Form:fields.html.twig' %}
{% form_theme delete_form 'MainBundle:Form:fieldsDelete.html.twig' %}

{% block body -%}
<div class="page" style="width:40%;margin:auto"><br>
    <h1 style="text-decoration:underline">Edition client</h1><br>

        {{ form(edit_form) }}
        <br>
        {{ form(delete_form) }}
        <br><br>
            <ul class="record_actions">
        <li>
            <a href="{{ path('client') }}">
                Retourner à la liste
            </a>
        </li>
    </ul>
</div>
{% endblock %}
