{% extends 'MainBundle:Back:baseBack.html.twig' %}
{% form_theme delete_form 'MainBundle:Form:fieldsDelete.html.twig' %}

{% block body -%}
<div class="page" style="width:40%;margin:auto">
    <h1 style="text-decoration:underline">Produit</h1><br>

    <table class="record_properties table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ entity.libelle }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ entity.description }}</td>
            </tr>
        </tbody>
    </table>
    {{ form(delete_form) }}
    <br><br><br>

        <ul class="record_actions">
            <li>
                <a href="{{ path('produit_edit', { 'id': entity.id }) }}">
                    Editer
                </a>
            </li>
            <li>
                <a href="{{ path('produit') }}">
                   Retourner à la liste
                </a>
            </li>
        </ul>
    </div>
{% endblock %}
