{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Home creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('home') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
