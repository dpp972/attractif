{% extends 'MainBundle:Back:baseBack.html.twig' %}

{% block body -%}
<div style="width:80%;margin:auto;text-align:center;padding-top:60px">
    <h1 style="text-decoration:underline">Home liste</h1><br>

    <table class="records_list table table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Slogan</th>
                <th>Help</th>
                <th>Email</th>
                <th>Nomsociete</th>
                <th>Telephone</th>
                <th>Presentation</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('home_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.slogan }}</td>
                <td>{{ entity.help }}</td>
                <td>{{ entity.email }}</td>
                <td>{{ entity.nomSociete }}</td>
                <td>{{ entity.telephone }}</td>
                <td>{{ entity.presentation }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('home_show', { 'id': entity.id }) }}">Voir</a>
                    </li>
                    <li>
                        <a href="{{ path('home_edit', { 'id': entity.id }) }}">Editer</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <a href="{{ path('ADMIN') }}">
        Retour à l'accueil
    </a>
        <!--ul>
        <li>
            <a href="{#{ path('home_new') }#}">
                Create a new entry
            </a>
        </li>
    </ul-->
</div>
    {% endblock %}
